# Fidoo Blog Web 前端架构设计

## 📐 架构原则

### 1. 性能优先
- **代码分割**: 路由级、组件级动态导入
- **图片优化**: Next.js Image + WebP/AVIF 格式
- **虚拟滚动**: 长列表使用 `@tanstack/react-virtual`
- **预加载**: 关键资源预加载，路由预取
- **缓存策略**: React Query 缓存 + Service Worker

### 2. 用户体验
- **骨架屏**: 所有异步内容都有骨架屏
- **渐进式加载**: 关键内容优先，次要内容延迟
- **流畅动画**: Framer Motion 实现流畅过渡
- **响应式设计**: 移动端优先，渐进增强
- **无障碍访问**: ARIA 标签，键盘导航

### 3. 代码质量
- **类型安全**: 严格的 TypeScript 配置
- **组件复用**: 原子化设计模式
- **关注点分离**: UI、逻辑、数据分离
- **可测试性**: 组件易于单元测试

## 🏗️ 目录结构

```
web/src/
├── app/                    # Next.js App Router 页面
│   ├── (auth)/            # 认证相关页面（路由组）
│   ├── (main)/            # 主要内容页面（路由组）
│   └── api/               # API Routes（如需要）
│
├── components/            # 组件库
│   ├── ui/               # 基础UI组件（shadcn/ui风格）
│   │   ├── button/
│   │   ├── card/
│   │   ├── dialog/
│   │   ├── skeleton/
│   │   └── ...
│   ├── layout/           # 布局组件
│   │   ├── Header/
│   │   ├── Sidebar/
│   │   ├── Footer/
│   │   └── Container/
│   ├── features/         # 功能组件
│   │   ├── post/
│   │   ├── category/
│   │   ├── tag/
│   │   └── user/
│   └── shared/           # 共享组件
│       ├── ErrorBoundary/
│       ├── Loading/
│       └── EmptyState/
│
├── hooks/                 # 自定义 Hooks
│   ├── useDebounce.ts
│   ├── useIntersection.ts
│   ├── usePrefetch.ts
│   └── ...
│
├── lib/                   # 工具库
│   ├── api/              # API 客户端
│   ├── utils/            # 工具函数
│   ├── constants/        # 常量
│   └── config/           # 配置
│
├── store/                # 状态管理
│   ├── auth.ts
│   └── ui.ts
│
├── styles/               # 样式
│   ├── globals.css
│   └── animations.css
│
└── types/                # 类型定义
    ├── api.ts
    ├── components.ts
    └── ...
```

## 🎨 设计系统

### UI 组件库
使用 **shadcn/ui** 作为基础组件库：
- 可定制、可访问
- 基于 Radix UI
- Tailwind CSS 样式

### 设计令牌
- **颜色系统**: 主色、语义色、中性色
- **间距系统**: 4px 基础单位
- **字体系统**: 字体大小、行高、字重
- **阴影系统**: 层级阴影
- **动画系统**: 缓动函数、持续时间

## ⚡ 性能优化策略

### 1. 代码分割
```typescript
// 路由级代码分割
const PostList = dynamic(() => import('@/components/features/post/PostList'), {
  loading: () => <PostListSkeleton />,
  ssr: true,
});

// 组件级代码分割
const HeavyChart = dynamic(() => import('@/components/features/chart/Chart'), {
  ssr: false, // 客户端渲染
});
```

### 2. 图片优化
- 使用 Next.js Image 组件
- 自动 WebP/AVIF 转换
- 响应式图片尺寸
- 懒加载 + 占位符

### 3. 数据获取优化
- **React Query**: 智能缓存、后台更新
- **预取**: 鼠标悬停时预取数据
- **流式渲染**: 使用 Suspense 边界
- **ISR**: 静态页面增量更新

### 4. 虚拟滚动
长列表使用 `@tanstack/react-virtual`:
```typescript
import { useVirtualizer } from '@tanstack/react-virtual';
```

## 🎯 用户体验优化

### 1. 加载状态
- **骨架屏**: 精确匹配内容布局
- **渐进式加载**: 关键内容优先
- **乐观更新**: 操作立即反馈

### 2. 错误处理
- **错误边界**: 组件级错误捕获
- **友好错误提示**: 清晰的错误信息
- **重试机制**: 自动重试失败请求

### 3. 动画和过渡
- **页面过渡**: 流畅的页面切换
- **微交互**: 按钮、卡片悬停效果
- **加载动画**: 优雅的加载指示器

## 📦 技术栈

### 核心框架
- **Next.js 14**: App Router, Server Components
- **React 18**: Concurrent Features, Suspense
- **TypeScript**: 类型安全

### UI 和样式
- **Tailwind CSS**: 实用优先的 CSS 框架
- **shadcn/ui**: 高质量组件库
- **Framer Motion**: 动画库
- **Radix UI**: 无样式、可访问组件

### 数据获取
- **TanStack Query**: 数据获取和缓存
- **Axios**: HTTP 客户端

### 状态管理
- **Zustand**: 轻量级状态管理
- **React Context**: 主题、配置等

### 工具库
- **date-fns**: 日期处理
- **zod**: 数据验证
- **clsx/tailwind-merge**: 类名合并

## 🚀 性能指标目标

- **LCP (Largest Contentful Paint)**: < 2.5s
- **FID (First Input Delay)**: < 100ms
- **CLS (Cumulative Layout Shift)**: < 0.1
- **TTI (Time to Interactive)**: < 3.5s
- **Bundle Size**: 初始加载 < 200KB (gzipped)

## 🔧 开发工具

- **ESLint**: 代码检查
- **Prettier**: 代码格式化
- **TypeScript**: 类型检查
- **Bundle Analyzer**: 打包分析

